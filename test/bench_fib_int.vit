function fib(n: int): int {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

function main(): int {
    let n = 38;
    let res = fib(n);
    print(res);
    return 0;
}
