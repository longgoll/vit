function fib(n: number): number {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

function main(): number {
    let n = 38;
    let res = fib(n);
    print(res);
    return 0;
}
